#include <stdio.h>

int main() {
    char str1[100], str2[100];

    scanf("%s %s", str1, str2);  // Reads two strings separated by space

    printf("%s and %s\n", str1, str2);

    return 0;
}
