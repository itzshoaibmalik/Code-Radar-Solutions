#include <stdio.h>

int main() {
    float str1, str2;

  
    scanf("%if %if", str1, str2);  // Reads two strings separated by space

    printf("You entered: %if, %if\n", str1, str2);

    return 0;
}
